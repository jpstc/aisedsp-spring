
spring:
  datasource:
    url: ${SQL_CONN}
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
server:
  port: 8081
management:
  endpoints:
    web:
      exposure:
        include: health,info
